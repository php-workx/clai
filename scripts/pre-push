#!/usr/bin/env bash
set -euo pipefail

# Pre-push: full quality gate — last safety net before CI.
# Install: make setup

export PATH="/opt/homebrew/bin:/usr/local/bin:$HOME/go/bin:$HOME/.cargo/bin:$PATH"

cd "$(git rev-parse --show-toplevel)"
make check
